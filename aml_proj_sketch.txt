open changes todo

- explanation should differentiate between values taken from user and values provided by default average --> prompt for explanation should provide default json and final json so gpt can look at differences to determine which one come from user 
- more sarcasm and less numerical values for output because user does not care about shap values, just results and relative importance of values
- plot shap diagrams inside the chat 